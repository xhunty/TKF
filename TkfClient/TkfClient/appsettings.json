{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "System.*": "Warning"
    }
  },
  "AccessToken": "",
  "NLog": {
    "internalLogFile": "c:\\temp\\client\\internal.log",
    "internalLogLevel": "Info",
    "targets": {
      "async": true,
      "fileTarget": {
        "type": "File",
        "fileName": "c:\\temp\\client\\log-${shortdate}.log",
        "layout": "${longdate}|${uppercase:${level}}|${logger}|${message}|${exception:format=tostring}"
      }
    },
    "rules": [
      {
        "logger": "TkfClient.*",
        "minLevel": "Info",
        "writeTo": "fileTarget"
      }
    ]
  }
}